
void testfmtprint() {	
	string s = "";

	s = sprintf("%j$l %j$c");
	Alert(s);
	s = sprintf("Hexoffset %j$O");
	Alert(s);
	CursorDown(MOT_SINGLE); // currently causing problems, as MOT_SINGLE is not defined. 
	s = sprintf("%j$l %j$c");
	Alert(s);
}

// 
// @param x a test param
// macro findtest(int x) {
//	Find(FORM_INIT, "^\\T*$", "", DIR_BACKWARD, REP_FIND, RNG_ONCE, 
//		RE_DOREX, 0);
//}

